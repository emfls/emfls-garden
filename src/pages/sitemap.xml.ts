import type { APIRoute } from 'astro'; import {plants} from '../data/plants'; import {guides,problems} from '../data/content';
export const GET: APIRoute = ({site}) => {const paths=['/','/plants/','/guides/','/problems/','/tools/plant-finder/','/tools/watering-guide/','/about/','/editorial-policy/','/privacy/','/contact/',...plants.map(p=>`/plants/${p.slug}/`),...guides.map(([s])=>`/guides/${s}/`),...problems.map(([s])=>`/problems/${s}/`)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${new URL(p,site)}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml'}})};

# emfls-garden 작업 규칙

- 대상은 `emfls/emfls-garden`과 `https://garden.emfls.com/`뿐이다.
- 다른 EMFLS 사이트의 디자인·코드·콘텐츠를 복사하지 않는다.
- 작업 시작 전 최신 Notion을 다음 순서로 확인한다: `EMFLS Network 관제탑` → `Sites → emfls-garden` → Garden `Site Status Sync` → `Network Baseline v1` → `Network QA` → `Automation Protocol` → `Network Master Plan` → GitHub Repo → `AGENTS.md` → `SITE_STRATEGY.md` → `DESIGN_SYSTEM.md` → `TASKS.md` → `PROJECT_HISTORY.md` → 실제 코드/Production.
- 실제 상태만 기록하며 `APPROVED`나 `LIVE`를 임의 선언하지 않는다.
- Site Status Sync는 중앙 Site Registry의 Garden 원본 행을 갱신하며 별도 복사본을 만들지 않는다.
- 검색등록, 매체별 sitemap, IndexNow, GA4, AdSense는 실제 확인값만 기록하고 추정하거나 placeholder를 넣지 않는다.
- 모든 세션은 코드 변경 여부와 관계없이 Notion Work History에 기록하고, 의미 있는 작업은 `PROJECT_HISTORY.md`에도 기록한다.
- Review 판단은 `READY_FOR_REVIEW`까지만 사용한다. `APPROVED` 또는 `LIVE 완료`를 선언하지 않는다.
